import 'package:flutter/material.dart';

class ExploreTile extends StatelessWidget {
  const ExploreTile({Key? key}) : super(key: key);
  // final Post post;

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.pink,
    );
  }
}
